

function concat (x, y) {
    var newarray = []
    var i = 0;
    while (i < x.length) {
       newarray.push(x[i]);
       ++i};
  var i = 0;
    while (i < y.length) {
       newarray.push(y[i]);
       ++i};


    return newarray  
  }
 
